<h1> All Recipes </h1>


<div id= "recipes">
    <ul>
        <% @recipes.each do |recipe| %>
        <li> <%= link_to recipe.name, recipe_path(recipe.id) %> </li>     
        <% end %>
    </ul>
</div>

<div id= "action"> <%=link_to "Create A New Recipe?", new_recipe_path %> </div>